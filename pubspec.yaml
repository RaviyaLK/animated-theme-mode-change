name: theme_mode_change
description: "This is a Flutter project based on Rive animation"
publish_to: "none"
version: 0.1.0

environment:
  sdk: ">=3.2.5 <4.0.0"

dependencies:
  adaptive_theme: ^3.6.0
  flutter:
    sdk: flutter

  go_router: ^13.1.0
  rive: ^0.12.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/
